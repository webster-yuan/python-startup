接口鉴权体系改造（FastAPI / Python）

参与公司接口鉴权体系改造，采用集中式授权架构，业务服务与权限策略解耦

基于 FastAPI 设计并接入自研 Python 鉴权 SDK，通过中间件 + 依赖注入方式在接口层声明所需权限码

SDK 从请求中提取登录态 sid，通过 RPC 调用中台 user-auth 服务获取用户权限集合，并在本地完成权限匹配，未授权请求返回 403

权限模型基于用户-角色-权限设计，权限码与前端页面操作及接口能力一一对应，由中台 portal 系统统一配置与管理