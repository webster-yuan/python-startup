多进程，多线程

进程：操作系统分配资源的基本单位，每打开一个程序就会启动一个进程
线程：CPU调度的基本单位，每个进程至少一个线程，主线程

进程状态：
1. 执行状态：CPU正在执行
2. 就绪状态：条件都准备好了，等待CPU调度
3. 等待状态：等待条件就绪

方法
start()：开启子进程
is_alive()：是否存活
join(): 等待子进程退出
name: 进程名称
pid: 子进程的编号

协程
单线程下开发，又称为微线程
线程和进程的操作，是由程序触发系统接口，最后的执行者是系统，协程的操作者是程序员
